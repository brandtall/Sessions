const assert = require('assert');

describe('Controllers', () => {
    describe('GET /', () => {
        it('Should return OK', () => {
            assert.ok(true);
        });
    });
    describe('GET /login', () => {
        it('Should return OK', () => {
            assert.ok(true);
        });
    });

    describe('GET /register', () => {
        it('Should return OK', () => {
            assert.ok(true);
        })
    })
});